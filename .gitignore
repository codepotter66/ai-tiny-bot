# OS / IDE
.DS_Store
.idea/
.vscode/
.claude/settings.local.json
*.swp
*~

# Secrets / local env
.env
.env.*
!.env.example
**/*.pem
**/*.key

# Archives / deploy artifacts
*.zip
*.tar
*.tar.gz

# Cloud agent build & data
tiny-bot-cloud-agent/bin/
tiny-bot-cloud-agent/data/
tiny-bot-cloud-agent/coverage.out
tiny-bot-cloud-agent/*.test

# Firmware build & private config
tiny-bot-firmware/.pio/
tiny-bot-firmware/include/config.h

# Cursor hook session state (edited-path tracker)
.cursor/hooks/state/

# Local debug evidence (not for git)
tmp/
